# コンパイル時、target/db/testdb.h2.dbというファイルにデータベースの情報が永続化される。
# そのため、もしdata.sqlを一度だけしか利用しない場合は、一度実行後に削除するなどの対応をする。
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:file:./target/db/testdb2
spring.datasource.username=sa
spring.datasource.password=